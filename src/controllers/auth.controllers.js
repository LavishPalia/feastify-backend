import { asyncHandler } from "../utils/asyncHandler.js";
import { User } from "../models/User.model.js";

const createCustomerAccount = asyncHandler(async (req, res, next) => {});

export { createCustomerAccount };
